<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= country.name %> Details</title>
    <link rel="stylesheet" href="/css/main.css" />
</head>

<body>
    <% include('partials/navbar', {page: ''}) %>

    <div class="container mx-auto">
        <div class="hero bg-base-200 rounded-lg p-6">
            <div class="hero-content text-center">
                <div class="max-w-md">
                    <h1 class="text-5xl font-bold"><%= country.name %></h1>
                    <p class="py-6">Below, you will find detailed information about the country: <%= country.name %></p>
                    <img src="<%= country.flag %>" alt="<%= country.name %> Flag" class="w-64 mx-auto mb-4">
                    <p><strong>Official Name:</strong> <%= country.officialName %></p>
                    <p><strong>Native Name:</strong> <%= country.nativeName %></p>
                    <!-- Accessing the region name property -->
                    <p><strong>Region:</strong> <%= country.region.name %></p>
                    <p><strong>Alpha 2 Code:</strong> <%= country.a2code %></p>
                    <p><strong>Capital:</strong> <%= country.capital %></p>
                    <p><strong>Population:</strong> <%= country.population %></p>
                    <a href="<%= country.wikipediaURL %>" class="btn btn-primary" target="_blank">Wikipedia</a>
                    <div id="quote" class="mt-4"></div>
                    <button class="btn btn-secondary mt-2" onclick="history.back(); return false;">Back</button>
                </div>
            </div>
        </div>
    </div>

    <script>
        document.addEventListener("DOMContentLoaded", () => {
            fetch("https://quotable.io/random")
                .then(response => response.json())
                .then(data => {
                    const quoteElement = document.getElementById("quote");
                    quoteElement.innerHTML = `<blockquote>"${data.content}"<br>- ${data.author}</blockquote>`;
                })
                .catch(error => console.error("Error fetching quote:", error));
        });
    </script>
</body>

</html>
